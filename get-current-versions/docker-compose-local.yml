services:
  version-scraper:
    build: .
    image: thales-version-scraper:${VERSION:-latest}
    container_name: thales-version-scraper
    ports:
      - "3210:80"
    environment:
      # Controls when the daily midnight scan runs.
      TZ: America/New_York
    restart: unless-stopped
